# Copy to .env.local and fill in. The publishable key is safe in a browser
# bundle: every table is closed to the Data API and each RPC checks the
# caller's session and role in the database.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
